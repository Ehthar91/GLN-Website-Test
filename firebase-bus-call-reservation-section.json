{
  "quizRooms": {
    ".read": "auth != null && query.orderByChild == 'hostUid' && query.equalTo == auth.uid",
    ".indexOn": [
      "hostUid"
    ],
    "$code": {
      ".read": "auth != null",
      ".write": "auth != null && ((!$code.matches(/^BUS_/) && ((!data.exists() && newData.child('hostUid').val() === auth.uid) || data.child('hostUid').val() === auth.uid)) || ($code.matches(/^BUS_/) && newData.exists() && ((!data.exists() && newData.child('hostUid').val() === auth.uid && newData.child('callerLastActiveAt').val() === now) || data.child('hostUid').val() === auth.uid || (((data.child('callerLastActiveAt').isNumber() && data.child('callerLastActiveAt').val() <= now - 2592000000) || (!data.child('callerLastActiveAt').exists() && ((data.child('lastActivityAt').isNumber() && data.child('lastActivityAt').val() <= now - 2592000000) || (!data.child('lastActivityAt').exists() && data.child('createdAt').isNumber() && data.child('createdAt').val() <= now - 2592000000)))) && newData.child('hostUid').val() === auth.uid && newData.child('callerLastActiveAt').val() === now && newData.child('createdAt').val() === now && newData.child('status').val() === 'open' && !newData.child('history').exists() && !newData.child('currentCall').exists() && !newData.child('players').exists()))))",
      "players": {
        "$uid": {
          ".write": "auth != null && (auth.uid === $uid || root.child('quizRooms').child($code).child('hostUid').val() === auth.uid)"
        }
      },
      ".validate": "!$code.matches(/^BUS_/) || (newData.child('hostUid').isString() && (!data.child('callerLastActiveAt').exists() || newData.child('callerLastActiveAt').isNumber()) && (!newData.child('callerLastActiveAt').exists() || (newData.child('callerLastActiveAt').isNumber() && newData.child('callerLastActiveAt').val() <= now && (!data.child('callerLastActiveAt').exists() || newData.child('callerLastActiveAt').val() >= data.child('callerLastActiveAt').val()))) && (newData.child('hostUid').val() === data.child('hostUid').val() || newData.child('callerLastActiveAt').val() === now))"
    }
  }
}
